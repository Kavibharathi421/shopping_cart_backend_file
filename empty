app.route("/empty"),method=["post"]
def add():
   data=request json
 register=Reghisre(
   name=data[name]

 )
order.route("/order-item")
def order_item():
   cart_item=Cart.query.all()
   result=[]
   for item in cart_item:
      result.append({
         "login_id":item.login.login_id
      })      